import * as React from 'react';

import './styles.css';

/**
 * Component
 */

const RoomsHeaderPreloader = () => (
  <header styleName="header">
    <div styleName="menu">
      <div styleName="button-mock"/>
      <div styleName="button-mock"/>
      <div styleName="button-mock"/>
    </div>
  </header>
);

/**
 * Export
 */

export default RoomsHeaderPreloader;
